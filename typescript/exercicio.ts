

const caluloAreaCampo = (largura: number, comprimento: number) => console.log(largura * comprimento);

const welcome = (nome: string) => console.log(`Olá ${nome}`);
caluloAreaCampo(20, 10);
welcome("Gustavo") ;